% COMMAND(1) open-tukubai-in-rust | User Commands
% open-tukubai-in-rust
%

# NAME

command - one line summary

# SYNOPSIS

**command** [*OPTION*...] [*OPERAND*...]

# DESCRIPTION

Describe what the command does.

Input is processed using the shared open-tukubai-in-rust parsing rules.
Records are separated only by LF (`0x0A`), fields are separated only by SPACE
(`0x20`), and the final record must be LF-terminated.

# OPTIONS

# OPERANDS

# EXIT STATUS

`0`
: Success.

`1`
: Error.

# EXAMPLES

```sh
command ...
```

# SEE ALSO

**lcnt**(1), **self**(1), **delf**(1)
